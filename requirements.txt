# 필수 - 이 항목만 있으면 핵심 8종 변환이 동작합니다.
lxml>=5.0            # HWPX(OWPML) XML 파싱/생성
olefile>=0.47        # HWP 5.0 / DOC / XLS 의 OLE 복합문서 판독
PyMuPDF>=1.24        # PDF 읽기·쓰기 (1.24+ 라야 글꼴 서브셋이 됩니다)
python-docx>=1.1     # DOCX 읽기·쓰기
openpyxl>=3.1        # XLSX 읽기·쓰기
XlsxWriter>=3.1      # XLSX 보조

# 선택 - 없어도 되지만 있으면 지원 범위가 넓어집니다.
xlrd>=2.0            # 구형 .xls 읽기
tkinterdnd2>=0.4     # GUI 끌어다 놓기
beautifulsoup4>=4.12 # HTML 읽기
striprtf>=0.0.26     # RTF 읽기
odfpy>=1.4           # ODT 보조

# Windows 전용 (선택) - MS Office 가 설치돼 있으면 .doc/.xls 를 다룰 수 있습니다.
pywin32>=306 ; sys_platform == "win32"
